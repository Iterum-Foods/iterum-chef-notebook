FIREBASE STORAGE SETUP - 5 MINUTES
═══════════════════════════════════════════════════════════

STEP 1: Enable Storage
───────────────────────
1. Open: https://console.firebase.google.com/project/iterum-culinary-app/storage

2. Click "Get Started" button

3. Choose "Start in production mode"

4. Click "Next"

5. Select location: "us-central1"

6. Click "Done"

✅ Storage enabled!

═══════════════════════════════════════════════════════════

STEP 2: Update Storage Rules
───────────────────────
1. In Storage page, click "Rules" tab

2. Replace all rules with this:

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /videos/{videoId} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 500 * 1024 * 1024
                   && request.resource.contentType.matches('video/.*');
    }
    match /thumbnails/{thumbId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

3. Click "Publish"

✅ Rules set!

═══════════════════════════════════════════════════════════

STEP 3: Get Firebase Config
───────────────────────
1. Click ⚙️ (gear icon) → "Project settings"

2. Scroll down to "Your apps" section

3. Find Web app (or add one if none exists)

4. Copy the firebaseConfig values:
   - apiKey
   - authDomain  
   - projectId
   - storageBucket
   - messagingSenderId
   - appId

5. PASTE THEM HERE (I'll update the file):

apiKey: 
authDomain:
projectId:
storageBucket:
messagingSenderId:
appId:

═══════════════════════════════════════════════════════════

STEP 4: Test Upload
───────────────────────
After I update the config:

1. Visit: https://iterum-culinary-app.web.app/upload.html

2. Upload a short video (test with your phone)

3. Watch it upload!

4. Check Firebase Console → Storage → Files
   You'll see your video!

═══════════════════════════════════════════════════════════

DONE! Videos will upload to Firebase! 🎥

Send me your Firebase config values and I'll update the file!

